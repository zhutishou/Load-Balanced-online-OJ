#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>

//该文件是用来存放用户编写后的内容

//实际使用中：我们利用提前写好的宏将header.cpp和tail.cpp文件整合在一起提交给编译运行服务器compile_server.cc然后调用compile_run.hpp

using namespace std;

class Solution
{
    public:
        bool isPalindrome(int x)
        {
            //code
            return true;
        }
};
